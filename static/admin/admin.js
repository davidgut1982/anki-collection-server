/**
 * admin.js — placeholder for admin console JavaScript.
 *
 * Wave 1 (A6–A9) will add:
 *   - fetch-based API calls to /admin/api/* endpoints
 *   - Browser panel: paginated card table, bulk-action checkboxes
 *   - Scheduling panel: due-queue counts, card-reset confirmation modal
 *   - DB & Media panel: media-check progress stream
 *   - Diagnostics panel: Chart.js review-rate and revlog heatmap
 *
 * Nothing functional is needed in this scaffold step (A1).
 */

/* eslint-disable no-console */
(function () {
  "use strict";
  // Placeholder — no-op on load.
  // console.debug("admin.js loaded");
})();
